__pycache__/
*.py[cod]
.venv/
.venv-*/
.DS_Store

# Credentials liegen in ausleihe-api/.env — hier nie eine eigene .env committen.
.env

# Datenexporte (enthalten Schüler-/Personendaten) – nie committen
*.pdf

# Excel-Bestandslisten: Binärdateien gehören nicht ins Git (keine Diffs, Bloat).
# Die zugehörigen Skripte + config.json bleiben versioniert.
*.xlsx

# Recoverable local copies written by the inventory commands.
bestand/backups/

# setuptools-Metadaten des editable-Installs
*.egg-info/

# setuptools/uv-Bauverzeichnisse (`uv build` beim Pruefen des py.typed-Rads)
build/
dist/
